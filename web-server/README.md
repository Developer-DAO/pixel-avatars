# Pixel Art: Web server

### Local development

Install all dependencies

```shell
yarn install
```

Setup `.env`
```shell
cp .env.example .env
```

Run local development server

```shell
yarn serve # Starts a server on localhost:3000
```
